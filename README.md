# VendingMachineApp
I developed a full-stack vending machine web application using **Angular (TypeScript), ASP.NET Core (C#), and SQL Server**. The system allows users to view products and make purchases while the backend processes transactions and updates inventory. This project involved building the frontend UI, REST APIs, and managing database operations.
